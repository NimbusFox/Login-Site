@using NimbusFox.Login_Site.CodeLibraries.Core
@model NimbusFox.Login_Site.Models.Core.Page

@if (Model.DisplayHeader) {

}

@foreach (var child in Model.Page.Children) {
    Html.RenderAction(child.DocumentTypeAlias, GlobalHelper.ContentBuilder, new { page = child });
}

@if (Model.DisplayFooter) {
    @Html.Partial("Core/Footer", Model)
}